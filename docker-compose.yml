services:
  bili-up:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bili-up
    restart: unless-stopped
    environment:
      TZ: Asia/Shanghai
    volumes:
      - ./config:/app/config
      - ./logs:/app/logs
    command: ["--config", "/app/config/config.json", "scheduler"]
