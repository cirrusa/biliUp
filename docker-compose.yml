services:
  bili-up:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bili-up
    restart: unless-stopped
    env_file:
      - .env
    environment:
      TZ: Asia/Shanghai
    volumes:
      - ./config:/app/config
      - ./logs:/app/logs
    command: ["scheduler"]
