services:
  bilitool-go:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bilitool-go
    restart: unless-stopped
    environment:
      TZ: Asia/Shanghai
    volumes:
      - ./config:/app/config
      - ./logs:/app/logs
    command: ["--config", "/app/config/config.json", "scheduler"]
